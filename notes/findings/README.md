# Findings

Dated records of what was measured, with their numbers and "measured on". A finding
says what is true and carries its evidence. What to do about it lives in `bd`; what
was decided because of it lives in `docs/decisions/`. Filenames are
`YYYY-MM-DD-<slug>.md`.
