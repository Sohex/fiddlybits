[[oracle]]
id = "sweep.aquaplanet_ape"
tier = 3
statistic = "jet latitude"
verdict_kind = "fail_bar"
threshold = "provisional: outside the APE ensemble range fails"
protocol = "ape"
protocol_system = "APE() as a Sourced constructor"

[[protocol]]
id = "ape"
system = "APE() as a Sourced constructor"
normalisation = "prescribed surface, equal total instellation"
