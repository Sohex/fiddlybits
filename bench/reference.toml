# The reference artifact of decision 0029. A commit that moves this hash carries an
# `answers: <mechanism>` line and updates this file in the same commit; the commit-msg
# hook refuses one without the other.
#
# The case is a stand-in until the short coupled case of M3 exists. When it does, the
# name and the hash below change and the mechanism does not. See decision 0043.

[case]
name = "kepler_sweep"
hash = "5806877ec3c59821cfe1caba07a983fd789cc21060869877c8f4d586cb80f7a8"
recorded_at = 2026-09-11
stands_in_for = "the short coupled case of decision 0029, which arrives with M3"
